# Redis Helm Values (Bitnami)

auth:
  password: "${REDIS_PASSWORD}"

architecture: standalone

master:
  persistence:
    size: 1Gi
  resources:
    requests:
      memory: 128Mi
      cpu: 100m
    limits:
      memory: 256Mi
      cpu: 200m
